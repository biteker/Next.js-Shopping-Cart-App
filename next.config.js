/** @type {import('next').NextConfig} */
const nextConfig = {
 reactStrictMode: true,
 images: {
  domains: [
    'img.freepik.com',
    'cdn1.thecomeback.com'
  ],
},
};

module.exports = nextConfig;
